function O=update_O(X,M,U_prev,lambda,q,C,N)
    O=zeros(2,N);
    r=zeros(2,N);
    for i=1:N
        %equacao 12
        r(i,:)=sum(((U_prev(i,:).^q).*(repmat(X(i,:),[C,1])-M'))/sum(U_prev(i,:).^q,2),2);
        %equacao 11
        O(i,:)=r(i,:).*(1-lambda/(2*norm(r(i,:))));
    end
end